import React from 'react';

const Special = ({ring}) => {
    console.log(ring)
    return (
        <div>
            <h3>Special</h3>
            <p>{ring}</p>
        </div>
    );
};

export default Special;